{
  "name": "pollie-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker that keeps Pollie's Gemini key out of the APK.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260801.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.80.0"
  }
}
